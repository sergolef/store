export interface User {
    email:string
    displayName:string
    token:String
}

export interface Address {
    firstName: string
    lastName: string
    street: string
    city: string
    state: string
    zipCode: string
}